<h2>Index of Commissions</h2>
  <% @commissions.each do |commission|  %>
    <div>
      <h4><%= link_to commission.title, commission_path(commission) %></h4>
      <p><%= commission.description %></p>
      <% if commission.amount %>
        <h4>£<%= commission.amount %></h4>
      <% else %>
        <p>No price range specified</p>
      <% end %>
    </div>
  <% end %>
